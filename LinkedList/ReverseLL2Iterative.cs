using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Rotate.LinkedList
{
    internal static class ReverseLL2Iterative
    {
        public static Node ReverseLL1(Node startNode)
        {
            Node newStart = startNode;
            if (startNode != null) 
            {
                Node temp = startNode;
                Node tail = null;
                //while()
                //{
                    
                //}
                //newStart = tail;
            } 
            return newStart;
        }


    }
}
